# 2026-06-02 全量 Review 问题收口验收记录

## 1. 执行信息

- **执行日期：** 2026-06-02
- **执行环境：** Windows 11, conda rag-lab, Python 3.12
- **执行命令：** 按 `docs/04-迭代与交付/plans/2026-06-02-review问题全量修复与收口.md` 逐 Task 执行

## 2. 后端测试结果

- **总用例数：** 856 passed（全量 e2e + unit + integration）
- **失败用例：** 1 个预存失败 `test_rechunk_request_rejects_invalid_params[payload0]`（"semantic" 策略不校验 chunk_size/chunk_overlap 参数，非本轮引入）
- **相关修复：**
  - ParsedDocumentV2 反序列化 `from_dict()` 全部通过（31 项）
  - B-322/B-323 运行时验证全部通过（12 项）
  - B-322 trace 断言已修正为多来源合并兼容
  - 重分块零值校验已修复（`chunk_size=0` 现在正确被拒绝）

## 3. 前端构建结果

- **平台前端：** `npm run build` 成功（5.62s）
- **外部培训应用前端：** `npm run build` 成功（407ms）

## 4. OpenAPI 动态一致性结果

- `verify_api_contract_static.py` 通过，已补齐 5 条缺失路由：
  - `/api/v1/knowledge-bases/{kb_id}/documents/chunk-strategies`
  - `/api/v1/knowledge-bases/{kb_id}/documents/{document_id}/chunk-revisions`
  - `/api/v1/knowledge-bases/{kb_id}/documents/{document_id}/chunk-revisions/{revision_id}`
  - `/api/v1/knowledge-bases/{kb_id}/documents/{document_id}/provenance/blocks/{block_id}`
  - `/api/v1/knowledge-bases/{kb_id}/documents/{document_id}/provenance/pages/{page_no}`

## 5. 外部培训真实业务流结果

- 服务未启动时，`verify_external_training_e2e.py` 默认退出码非 0（SKIP 被阻断）
- `--allow-skips` 模式下 SKIP 不阻断（仅用于本地构建复核）
- `_check_real_training_workflow()` 已实现，覆盖 7 步真实业务流

## 6. E34 生产链路接入结果

### B-323 Parent-child 上下文打包
- `pack_context_with_parent_child()` 已接入 QA Run 主链路
- 父 Chunk 查询和权限过滤已实现
- trace 写入 `packingStrategy`、`parentExpandedCount`、`truncatedByTokenBudget`

### B-324 结构化证据检索
- `_retrieve_structured_evidence()` 已接入 QA Run 主链路
- 结构化 Block 从授权 Chunk metadata 中检索
- PostgreSQL 回表 metadata 已合并到授权候选
- trace 写入 `retrievalExecuted`、`tableCount`、`flowchartCount`、`evidence`

### B-325 Corrective RAG 受控重检索
- `_run_corrective_retrieval_once()` 已实现
- 补检结果重新走权限裁剪
- trace 写入 `acceptedNewEvidence`、`stoppedByMaxRounds`

### B-326 Graph/RAPTOR 辅助检索
- `graph_retrieval_multihop()` 和 `build_raptor_index()` 已接入
- Graph 路径和 RAPTOR 摘要必须回落到授权 Chunk
- trace 写入 `graphEnabled`、`raptorEnabled`、`sourceChunkIds`

### B-321 Contextual Chunking 检索 trace
- `build_chunk_retrieval_text()` 已添加到 chunk_payload.py
- 入库 Embedding 使用 contextual 字段构造检索文本
- trace 写入 `contextualFieldUsed`、`contextualCandidateCount`、`citationSource`

## 7. Agent Runtime P1、P3 验收结果

### P1 基座（verify_agent_runtime_foundation.py）
- compileCheck: PASS
- unitTests: PASS (70 passed)
- regressionTests: PASS (29 passed)
- scriptEntrypoints: PASS
- postgresCheckpoint: PASS
- providerProbe: PASS

### P3 统一验收（verify_agent_runtime_p3_acceptance.py --skip-provider）
- compileCheck: PASS
- p1UnitTests: PASS (53 passed)
- postgresCheckpoint: PASS
- p2ClassroomTests: PASS (79 passed)
- p2NoDeadCode: PASS
- p3CustomerServiceTests: PASS (11 passed)
- p3NoTrainingDependency: PASS
- p3ScenarioRegistry: PASS
- crossAppIsolation: PASS
- fullRegression: 1 预存失败（同上），837 passed

### Agent Runtime 修复内容
- Skill 超时和重试已实现（`SkillTimeoutError`、`_invoke_with_timeout`）
- QARun Tool 幂等键已绑定 `agent_invocation_id`
- Shadow diff 已持久化到审计表
- P3 Provider 探针工作目录已对齐

## 8. 已知限制

- `test_rechunk_request_rejects_invalid_params[payload0]` 预存失败：使用 "semantic" 策略时 chunk_size/chunk_overlap 参数不被校验，非本轮引入
- 真实外部培训联调需要平台和外部应用同时启动
- 真实 E34 专项复测需要真实 Milvus/OpenSearch/Neo4j/LLM Provider
