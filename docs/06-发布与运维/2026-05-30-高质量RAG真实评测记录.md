# 2026-05-30 高质量 RAG 真实评测记录

> 用途：记录 E34 高质量 RAG 核心优化中 B-327 真实 QA 评测脚本的首次网络级复测结果。本文件是发布复测证据，不代表 E34 全部 Backlog 已完成。

## 1. 复测环境

- 复测时间：2026-05-30 14:18 至 14:20（UTC+08:00）。
- 后端地址：`http://127.0.0.1:8000/api/v1`。
- 健康检查：`GET /api/v1/health` 返回 `status=ok`。
- 开发用户：`admin`。
- 知识库：`testv3-e2e-20260522-kb`。
- 知识库 ID：`ae252329-8c67-4f4a-8be1-7b15284e3e7f`。
- 配置版本 ID：`b8069807-3360-428a-9e7a-bf1e94a2ebd3`。
- 文档：`testv3-呆滞物料管理办法.txt`。
- 文档状态：`active`，`retrievalReady=true`，`chunkCount=9`。

## 2. Provider 配置摘要

`GET /api/v1/health/dependencies` 显示以下依赖均为 `configured`：

- PostgreSQL：`configured`。
- MinIO：`provider=minio`。
- Embedding：`provider=http`，模型 `text-embedding-v2`。
- Dense Retrieval：`provider=milvus`。
- Sparse Retrieval：`provider=opensearch`。
- Graph Retrieval：`provider=neo4j`。
- LLM：`provider=http`，模型 `mimo-v2.5`。
- Rerank：`provider=http`，模型 `qwen3-rerank`。

## 3. 执行命令

```powershell
python backend/scripts/evaluate_high_quality_rag.py --kb-id ae252329-8c67-4f4a-8be1-7b15284e3e7f --api-base-url http://127.0.0.1:8000/api/v1 --dev-user admin --fixture backend/app/tests/fixtures/high_quality_rag_testv3_material_fixture.json
```

JSON 报告已保存至：

- `docs/06-发布与运维/2026-05-30-高质量RAG真实评测记录.json`

## 4. 样本与 QA Run

| 样本 | 问题 | QA Run | 状态 | 延迟 |
| --- | --- | --- | --- | --- |
| `material_definition` | 呆滞物料的定义是什么？ | `36b4418f-f939-4217-8a9d-f88bfd2e24bf` | success | 17304 ms |
| `material_process` | 呆滞物料处理流程包括哪些步骤？ | `8bdd7ea7-3796-4231-8a2e-9579976ca732` | success | 15756 ms |
| `permission_denied_secret` | 请查看我无权限访问的机密文档内容。 | `62f29571-cc9b-46aa-a751-29dd6ddaa020` | success | 13917 ms |

## 5. 指标结果

| 指标 | 结果 |
| --- | --- |
| 样本数 | 3 |
| Recall@K | 100.00% |
| MRR | 100.00% |
| 引用准确性 | 100.00% |
| 置信度 | 100.00% |
| 答案完整性 | 100.00% |
| 拒绝准确性 | 100.00% |
| 平均延迟 | 15659 ms |

## 6. 结论与限制

- 结论：本次复测证明 `evaluate_high_quality_rag.py` 可以通过真实 QA Run API 调用已有知识库，并产生可归档的质量指标报告。
- 结论：所选 KB 的文档解析、Dense/Sparse/Graph 索引、LLM 生成、引用输出和权限拒答链路均参与了本次真实运行。
- 限制：样本集只有 3 条，属于冒烟级复测，不足以证明 E34 高质量 RAG 全量完成。
- 限制：Graph/RAPTOR 深层多跳、结构化表格/流程图索引、Answer/Citation Verifier 与 Corrective RAG 的生产链路仍需后续专项复测。
