# V1.7 Provider 网络级复测记录

## 1. 基本信息

- 复测环境：待填写
- 执行人：待填写
- 执行时间：待填写
- 后端版本或提交：待填写
- 配置来源：环境变量 / 受控配置 / 其他
- 关联版本：V1.7 RAG 模块化调优

## 2. 复测范围

本记录用于 V1.7 发布前确认真实 Provider 网络级可用性。复测对象包括 LLM、Embedding、Milvus、OpenSearch、Neo4j 和 Rerank。

`/api/v1/health/dependencies` 只确认配置摘要，不主动发起真实网络探测；真实连通性、鉴权、限流和响应格式必须通过显式诊断、真实入库、真实 QA 或人工网络探测记录完成。

## 3. 配置检查

执行命令：

```powershell
cd C:\Users\Public\Documents\Code\jin\rag-lab\backend
conda run -n rag-lab python scripts/verify_sprint15_provider_readiness.py
```

记录项：

| Provider | 目标配置 | 配置状态 | 脱敏检查 | 备注 |
| --- | --- | --- | --- | --- |
| LLM | `RAG_LAB_LLM_PROVIDER`、`RAG_LAB_LLM_ENDPOINT`、`RAG_LAB_LLM_MODEL` | 待复测 | 待复测 | 不记录明文 API Key |
| Embedding | `RAG_LAB_EMBEDDING_PROVIDER`、`RAG_LAB_EMBEDDING_ENDPOINT`、`RAG_LAB_EMBEDDING_MODEL` | 待复测 | 待复测 | 不记录明文 API Key |
| Rerank | `RAG_LAB_RERANK_PROVIDER`、`RAG_LAB_RERANK_ENDPOINT` | 待复测 | 待复测 | `identity` 只能作为降级记录，不能代表真实复测通过 |
| Milvus | `RAG_LAB_DENSE_RETRIEVAL_PROVIDER`、`RAG_LAB_MILVUS_URI`、`RAG_LAB_MILVUS_COLLECTION` | 待复测 | 待复测 | token 仅记录是否配置 |
| OpenSearch | `RAG_LAB_SPARSE_RETRIEVAL_PROVIDER`、`RAG_LAB_OPENSEARCH_HOSTS`、`RAG_LAB_OPENSEARCH_INDEX` | 待复测 | 待复测 | 账号密码仅记录是否配置 |
| Neo4j | `RAG_LAB_GRAPH_RETRIEVAL_PROVIDER`、`RAG_LAB_NEO4J_URI`、`RAG_LAB_NEO4J_DATABASE` | 待复测 | 待复测 | 密码仅记录是否配置 |

## 4. 网络连通性与限流诊断

接口：

- `GET /api/v1/health/provider-diagnostics`
- `GET /api/v1/health/dependencies`

记录项：

| Provider | connectivityStatus | rateLimitStatus | 响应格式 | 结果 | 处理建议 |
| --- | --- | --- | --- | --- | --- |
| LLM | 待复测 | 待复测 | 待复测 | 待复测 | 记录鉴权、超时、限流和模型不可用原因 |
| Embedding | 待复测 | 待复测 | 待复测 | 待复测 | 确认向量维度与 Milvus collection 一致 |
| Rerank | 待复测 | 待复测 | 待复测 | 待复测 | 记录降级为 `identity` 的风险 |
| Milvus | 待复测 | 不适用 | 待复测 | 待复测 | 确认 collection 存在、schema 匹配、可 search |
| OpenSearch | 待复测 | 不适用 | 待复测 | 待复测 | 确认 index 存在、mapping 可写可查 |
| Neo4j | 待复测 | 不适用 | 待复测 | 待复测 | 确认 database 可连接、实体关系可写可查 |

## 5. 真实入库与检索链路复测

执行命令：

```powershell
cd C:\Users\Public\Documents\Code\jin\rag-lab\backend
conda run -n rag-lab python scripts/verify_v16_real_rag_e2e.py
conda run -n rag-lab python scripts/verify_v17_pipeline_params.py
conda run -n rag-lab python scripts/verify_v17_pipeline_evaluation.py
```

记录项：

| 链路 | 覆盖 Provider | 预期证据 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| 文档解析与 Embedding | Embedding | 真实样例文档生成 Chunk 和向量 | 待复测 | 失败时记录 `V16EnvironmentLimit` 或代码缺口 |
| Dense 检索 | Milvus | QA Trace 包含 Dense 候选和 PostgreSQL 回表 Evidence | 待复测 | 不允许伪造本地候选作为通过 |
| Sparse 检索 | OpenSearch | QA Trace 包含 Sparse 候选和 PostgreSQL 回表 Evidence | 待复测 | 记录 index/mapping 问题 |
| Graph 检索 | Neo4j | QA Trace 包含 Graph 候选、支撑 Chunk 回落和权限裁剪 | 待复测 | 图实体不能直接作为最终 Evidence |
| Fusion 与 Rerank | Rerank | Trace 包含融合、重排结果和耗时 | 待复测 | `identity` 降级需明确标记 |
| Query Rewrite 与 Generation | LLM | Trace 包含 Query Rewrite、Generation、Citation | 待复测 | 记录模型、耗时、失败原因 |
| V1.7 参数与快照 | 全链路 | QARun 保存 `pipelineSnapshot` 和 `nodeParamSnapshot` | 待复测 | 确认参数来自目标 Revision |
| V1.7 评估对比 | 全链路 | P10/评估脚本展示命中、引用、耗时、失败原因差异 | 待复测 | 优化建议只作为可复核草稿 |

## 6. 检索副本重建复测

接口：

- `POST /api/v1/knowledge-bases/{kbId}/index-sync-jobs/rebuild`

记录项：

| targetStore | scope | status | errorMessage | 处理建议 |
| --- | --- | --- | --- | --- |
| milvus | kb / document / version | 待复测 | 待复测 | 确认向量删除、重建和版本切换后的状态 |
| opensearch | kb / document / version | 待复测 | 待复测 | 确认文本索引删除、重建和过滤字段 |
| neo4j | kb / document / version | 待复测 | 待复测 | 确认实体、关系和 ChunkRef 可重建 |

## 7. 发布判断

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| 真实 Provider 是否全部可达 | 待复测 | 任一 Provider 不可达需记录为发布风险 |
| 是否存在 local/mock 替代真实成功 | 待复测 | 存在则不能标记真实环境通过 |
| 是否存在鉴权、限流或响应格式风险 | 待复测 | 需记录影响范围和负责人 |
| V1.7 参数快照和评估对比是否可追溯 | 待复测 | 需能定位 ConfigRevision、QARun 和 EvaluationRun |

## 8. 遗留风险

| 风险 | 影响 | 负责人 | 后续动作 |
| --- | --- | --- | --- |
| 真实 Provider 复测尚未执行 | V1.7 只能证明源码与本地验证通过，不能证明目标环境网络级可用 | 待填写 | 在测试或预生产环境补齐本记录 |
