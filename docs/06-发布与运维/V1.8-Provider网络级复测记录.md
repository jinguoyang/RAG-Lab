# V1.8 Provider 网络级复测记录

用途：本文是 V1.8 Sprint 32 的真实 Provider 网络级复测记录，作为发布运维证据。后续同版本复测应更新本文和同目录 JSON 结果文件；不要用 local/mock/identity 结果替代真实网络结论。

## 1. 执行信息

- 执行时间：2026-05-16
- 执行环境：当前开发环境，已配置真实 HTTP 模型服务、Milvus、OpenSearch、Neo4j 和 Rerank 服务。
- 执行命令：

```powershell
cd backend
conda run -n rag-lab python scripts/verify_provider_network_retest.py --output ..\docs\06-发布与运维\V1.8-Provider网络级复测记录.json --strict
```

## 2. 复测结论

| Provider | 配置类型 | 结果 | 耗时 | 证据摘要 |
| --- | --- | --- | --- | --- |
| Embedding | `http` | success | 969ms | 模型 `text-embedding-v2`，向量维度 1536 |
| Milvus | `milvus` | success | 1615ms | Collection `default` 存在 |
| OpenSearch | `opensearch` | success | 383ms | 集群 `docker-cluster` 可访问，索引 `rag-lab` 存在 |
| Neo4j | `neo4j` | success | 546ms | 数据库 `neo4j` 连接校验通过 |
| LLM | `http` | success | 11569ms | 模型 `qwen3.5-flash` 返回有效内容 |
| Rerank | `http` | success | 528ms | 模型 `qwen3-rerank` 返回 1 条重排结果 |

JSON 原始证据见 [V1.8-Provider网络级复测记录.json](./V1.8-Provider网络级复测记录.json)。该文件不包含 Provider API Key、连接密码或完整请求正文。

## 3. 风险与后续动作

- 本次复测确认当前环境的真实 Provider 网络、鉴权和基础响应格式可用。
- 当前未配置正式发布 SLA 阈值，本记录保留轻量探测耗时作为后续压测和告警阈值的基线。
- 本脚本只做轻量网络探测，不替代完整真实文档入库、真实检索、Citation 和 App Runtime 端到端验收。
- Sprint 35 另以 `backend/scripts/verify_app_runtime_real_provider_e2e.py` 完成真实文本上传、真实入库、App Runtime 调用和 PostgreSQL Chunk Citation 回查；本次通过记录的 `runId` 为 `8d2fe304-0e01-45ce-ab2e-73618ca89021`，`citationCount=1`。
- 若后续切换测试或预生产环境，必须重新运行脚本并更新本记录，不能沿用本地或旧环境结论。
