# 验收清单

## 1. 业务验收项

| 编号 | 验收项 | 验收标准 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| A-001 | 文档中心可完成上传与重解析演示 | 上传后有文档对象与 ingest 任务反馈，批量重解析有确认 | 通过 | `verify_sprint12_acceptance.py` 覆盖上传、重解析和 ingest Worker 契约；真实界面演示仍需联调环境复核 |
| A-002 | 文档详情支持版本切换与权限脱敏演示 | 可切换 active version，可展示无权限正文脱敏态 | 通过 | `verify_sprint12_acceptance.py` 覆盖版本切换二次确认、Chunk 读取权限和 active version 裁剪 |
| A-003 | 配置中心可保存并激活 revision | 保存与激活语义清晰分离 | 通过 | `verify_sprint12_acceptance.py` 覆盖 validate、save、activate 接口契约和后端二次校验 |
| A-004 | QA 调试支持异常与回溯 | 可展示成功、部分降级、权限裁剪三类场景 | 通过 | `verify_sprint12_acceptance.py` 覆盖 QARun 执行、Provider 降级诊断、permissionFiltered 和 pipelineParams 快照 |
| A-005 | QA 历史支持回放与人工标注 | 历史记录可带上下文回放到调试页 | 通过 | `verify_sprint12_acceptance.py` 覆盖历史详情、反馈更新、回放上下文和 EvaluationSample 沉淀入口 |
| A-006 | 发布验收链路可追溯 | 审计日志、OpenAPI、依赖健康检查和运维手册可用于发布排障 | 通过 | Epic10 已补齐最小治理入口 |

## 2. 技术验收项

| 编号 | 验收项 | 验收标准 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| T-001 | 工程可运行 | 可完成安装、启动和构建 | 通过 | 后端 `compileall`、Sprint 12 验收脚本和前端 Vite 构建作为发布前最小检查 |
| T-002 | 文档完整性 | 需求、原型、设计、测试文档可闭环引用 | 通过 | 产品待办、Sprint 12 计划、测试计划和本验收清单已回填 Sprint 12 验证口径 |
| T-003 | 接口与审计治理 | OpenAPI 可生成，`/audit-logs` 和 `/health/dependencies` 已纳入 Schema | 通过 | `verify_epic10_release_ops.py` 覆盖 |
| T-004 | 发布检查脚本 | 后端 Epic10 验证脚本和 OpenAPI 导出脚本可执行 | 通过 | 使用 Conda 环境 `rag-lab` |

## 3. 遗留问题

- 当前验收结果基于 OpenAPI、源码护栏、后端编译和前端构建；真实外部 Provider 的连通性需在接入对应环境后补充网络级探测。
